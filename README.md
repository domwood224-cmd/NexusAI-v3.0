Here are all the generated files.

## Build and Run

Use the provided helper scripts to compile and execute the project.

- `./build.sh` - compile all Java sources into `bin/`
- `./run.sh <MainClass> [args...]` - run a compiled Java class from `bin/`
- `./clean.sh` - remove build outputs

Example:

    ./build.sh
    ./run.sh com.nexusai.assistant.ai.advanced.ActiveLearningSystem
